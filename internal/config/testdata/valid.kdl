wirecmd {
    root "/workspace"
    mcp "memory" {
        scope "workspace"
        stdio "go" {
            arg "run"
            arg "./cmd/memory"
            env PLAIN="literal"
            env TOKEN=(secret)"env://MEMORY_TOKEN"
        }
    }
}
